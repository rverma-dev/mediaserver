# Media and torrent data
data/

# Logs
config/*/logs/
config/**/logs/
config/*/log/
config/**/log/
*.log
*.log.*

# Jellyfin heavy dirs (cache, transcodes, metadata regenerated)
config/jellyfin/cache/
config/jellyfin/metadata/
config/jellyfin/transcodes/
config/jellyfin/data/
config/jellyfin/root/

# Backups (services create their own)
config/*/Backups/
config/bazarr/config/backup/

# WARP state (auto-generated on startup)
config/warp/

# Caddy state (auto-generated certs, data)
# Caddy (Caddyfile tracked, state ignored below)

# Gluetun (removed, leftover)
config/gluetun/

# Prowlarr indexer definitions (bundled with image, regenerated)
config/prowlarr/Definitions/

# SQLite journal/wal files (transient)
*-shm
*-wal
*-journal

# Bazarr (backup, cache, db, transient, generated config)
config/bazarr/backup/
config/bazarr/cache/
config/bazarr/db/
config/bazarr/config/config.yaml
config/bazarr/config/analytics_visitor_id.txt
config/bazarr/config/announcements.json
config/bazarr/config/releases.txt
config/bazarr/config/throttled_providers.dat

# Jellyfin marker files
.jellyfin-*

# Lock/pid files (transient)
*.pid
*.lock
lockfile
storage.lock

# GeoDB (bundled, large binary)
config/qbittorrent/qBittorrent/GeoDB/

# ASP data protection keys (regenerated)
config/*/asp/

# Log databases (regenerated)
logs.db

# qBittorrent torrent resume data (transient)
config/qbittorrent/qBittorrent/BT_backup/

# Media cover art (regenerated from metadata)
config/*/MediaCover/

# Seerr (cache, db, transient)
config/seerr/cache/
config/seerr/db/
config/seerr/anime-list.xml
config/seerr/settings.old.json

# SSH key (never commit)
GitHub-private-rv

# Secrets
.env

# OpenClaw runtime state (sessions, credentials, device tokens)
config/openclaw/

# OpenClaw workspace runtime
openclaw/workspace/.openclaw/

# Generated configs (from templates + .env)
config/sonarr/config.xml
config/radarr/config.xml
config/prowlarr/config.xml

# Service databases (transient, regenerated)
*.db
*.db-shm
*.db-wal
config/*/db/
config/qbittorrent/qBittorrent/qBittorrent-data.conf

# Generated configs (from templates via init-config.sh)
config/sonarr/config.xml
config/radarr/config.xml
config/prowlarr/config.xml
config/bazarr/config/config.yaml

# Caddy TLS certs and state (auto-generated by ACME)
config/caddy/data/
config/caddy/config/
config/caddy/cert.pem
config/caddy/key.pem
# Track: config/caddy/Caddyfile

# Nix
result
result-*
.direnv/

# Sentry crash telemetry
config/*/Sentry/
